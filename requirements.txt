# Pinned for reproducibility. Tested on Python 3.11+.
# numpy/pandas/scipy/matplotlib pinned to the versions the analysis was verified on.
# yfinance pinned because its API has had breaking changes across 0.2.x -> 1.x.
yfinance==0.2.66
numpy==2.4.4
pandas==3.0.2
scipy==1.17.1
matplotlib==3.10.8
jupyter==1.1.1
jupyterlab==4.5.7
# PDF generation for reports/Risk Report.pdf (build_risk_report_pdf.py).
# xhtml2pdf is pure Python (no native GTK/Pango deps), so the build
# script works identically on Windows, macOS and Linux.
markdown==3.7
xhtml2pdf==0.2.17